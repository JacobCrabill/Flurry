.{
  .core = .{
      .n_dims = 2,
      .mesh_file = "x.mesh",
      .order = 3,
  },

  .equation = .{
      .equation = .euler_ns,
  },

  .time = .{
      .dt_scheme = .rk44,
      .n_steps = 1000,
      .dt = 1e-6,
  },

  .output = .{
      .output_prefix = "out",
      .write_freq = 100,
      .report_freq = 50,
  },

  .test_case = .{
      .test_case = .uniform,
      .err_field = 0,
      .n_qpts_1d = 5,
  },

  .flux = .{
      .fconv_type = .rusanov,
      .fvisc_type = .ldg,
      .rus_k = 0.0,
      .ldg_b = 0.5,
      .ldg_tau = 1.0,
      .spt_type = .legendre,
  },

  .gas_properties = .{
      .T_gas = 291.15,
      .gamma = 1.4,
      .R = 286.9,
      .prandtl = 0.72,
      .S = 120.0,
  },

  .freestream = .{
      .rho_fs = 1.4,
      .P_fs = 1.0,
      .mach_fs = 0.2,
      .Re_fs = 200.0,
      .L_fs = 1.0,
      .T_fs = 300.0,
      .norm_fs = [1.0, 0.0, 0.0],
      .fix_vis = false,
  },

  .wall_conditions = .{
      .mach_wall = 0.0,
      .T_wall = 300.0,
      .norm_wall = [1.0, 0.0, 0.0],
  },

  .boundary_conditions = .{
      .mesh_bounds = {
          "inlet": .sup_in,
      },
  },

  .signals = .{
      .catch_signals = false,
  },

  .create_mesh = .{
      .nx = 4,
      .ny = 8,
      .nz = 2,
      .xmin = -1.0,
      .xmax = 1.0,
      .ymin = 0.0,
      .ymax = 4.0,
      .zmin = -0.5,
      .zmax = 0.5,
      .bc_bottom = .slip_wall,
      .bc_top = .symmetry,
      .bc_left = .sup_in,
      .bc_right = .sup_out,
      .bc_front = .characteristic,
      .bc_back = .periodic,
  },
}
